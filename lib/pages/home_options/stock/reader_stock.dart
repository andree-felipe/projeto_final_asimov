import 'package:flutter/material.dart';

class ReaderStock extends StatelessWidget {
  const ReaderStock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}